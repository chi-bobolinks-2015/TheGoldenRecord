$(document).ready(function(event){
  var activeMix = startMixer();
  setUserEvents(activeMix);

  $(".cell").on("drop", function(event){

  })

  // loadSound(activeMix, 5, 1)
  // loadSound(activeMix, 6, 2)
})

